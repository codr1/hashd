[project]
name = "hashd"
version = "0.1.0"
description = "Human-Agent Synchronized Handoff Development"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.0.0",
    "pyyaml>=6.0.0",
    "textual>=0.86.0",
    "prefect>=3.0.0",
    "pytest>=8.0.0",
    "transitions>=0.9.3",
]
